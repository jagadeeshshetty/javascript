{
  "name": "javascript",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "mocha test/**/*.js",
    "test:single": "mocha",
    "test:coverage": "nyc --reporter=text npm test",
    "test:coverage:report": "nyc --reporter=text-summary --reporter=text --reporter=lcov npm test",
    "test:coverage:report:lcov": "nyc --reporter=lcov --reporter=text-lcov npm test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^9.2.0",
    "nyc": "^15.1.0"
  }
}
